# MatchmakingAndLobbyService

The majority of the work is still backend, so the project does not do very much as of right now. Clients can connect to the server for the time being

Setup:
1. Open solution
2. Run sever exe
3. Run any number of client exe

